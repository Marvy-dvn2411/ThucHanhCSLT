#include<stdio.h>
#include<string.h>
#define MAX_LEN 100
int main(){
	char s1[MAX_LEN],s2 [MAX_LEN];
	int len1,len2;
	printf("Nhap vao a=");
	scanf("%s",s1);
	printf("Nhap vao b=");
	scanf("%s",s2);
	int result = strcasecmp (s1, s2);
	if (result < 0)
		printf("%s < %s",s1,s2);
	else if (result > 0)
		printf("%s > %s",s1,s2);
	else 
		printf("%s = %s",s1,s2);
	return 0;
}
